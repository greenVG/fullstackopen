// created by myself 

import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// Rendering principale dell'app React
ReactDOM.createRoot(document.getElementById('root')).render(<App />);
